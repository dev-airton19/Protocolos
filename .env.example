# Configuração do Banco de Dados PostgreSQL
# No Render, copie a URL de conexão do PostgreSQL e cole aqui
DATABASE_URL=postgres://usuario:senha@host:5432/nome_do_banco

# Chave secreta para sessões (será gerada automaticamente pelo Render)
SESSION_SECRET=sua_chave_secreta_aleatoria_aqui

# Porta do servidor (3001 ou a porta fornecida pelo Render)
PORT=3001
